# The consolidated artifact contains every held-out pair. Keep local checkpoints
# and bulky per-request traces for resume/debugging without duplicating them in Git.
pair-*.json
practice-[0-9]*.json
requests/
request-ledger.jsonl
