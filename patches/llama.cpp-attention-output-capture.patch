diff --git a/src/models/qwen3.cpp b/src/models/qwen3.cpp
--- a/src/models/qwen3.cpp
+++ b/src/models/qwen3.cpp
@@ -112,6 +112,7 @@ llama_model_qwen3::graph::graph(const llama_model & model, const llm_graph_para
             cur = build_attn(inp_attn,
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
+            cb(cur, "attn_out", il);
         }
         if (il == n_layer - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -239,7 +239,7 @@ static bool ik_tensor_capture_name_allowed(
     static const std::vector<std::string> stages = {
         "layer_inp", "attn_norm", "Qcur", "Kcur", "Vcur",
         "Vcache", "kq_soft_max", "kqv", "kqv_out", "ffn_inp", "ffn_norm",
-        "ffn_out", "l_out"
+        "attn_out", "ffn_out", "l_out"
     };
     for (const auto & stage : stages) {
         const std::string prefix = stage + "-";
