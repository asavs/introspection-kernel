diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -2515,6 +2515,7 @@ ggml_tensor * llm_graph_context::build_attn_mha(
 
     q = ggml_view_4d(ctx0, q, q->ne[0], q->ne[1], q->ne[2]/n_stream, n_stream, q->nb[1], q->nb[2], q->nb[3]/n_stream, 0);
 
+    cb(v, "Vcache", il);
     q = ggml_permute(ctx0, q, 0, 2, 1, 3);
     k = ggml_permute(ctx0, k, 0, 2, 1, 3);
     v = ggml_permute(ctx0, v, 0, 2, 1, 3);
diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -229,5 +229,5 @@ static bool ik_tensor_capture_name_allowed(
     static const std::vector<std::string> stages = {
         "layer_inp", "attn_norm", "Qcur", "Kcur", "Vcur",
-        "kq_soft_max", "kqv", "kqv_out", "ffn_inp", "ffn_norm",
+        "Vcache", "kq_soft_max", "kqv", "kqv_out", "ffn_inp", "ffn_norm",
         "ffn_out", "l_out"
     };
